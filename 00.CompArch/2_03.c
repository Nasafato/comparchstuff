#include <stdio.h>

int printf(const char * fmt, ...);
int printf(FILE *, const char * fmt, ...);
int sprintf(char * string, const char * fmt, ...);
int snprintf(char * string, sizet size, const char * fmt, ...);
